import "./Icon.scss";

import Icon, { iconColors, iconSizes, icons } from "./Icon";

const iconTypes = Object.values(icons);

export { iconTypes, iconColors, iconSizes, icons };

export { Icon as default };
